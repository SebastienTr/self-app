export {
  moduleSpecSchema,
  moduleTypeEnum,
  templateEnum,
  moduleStatusEnum,
  dataSourceSchema,
} from './moduleSpec';

export type { ModuleSpec, DataSource } from './moduleSpec';

export const CURRENT_SCHEMA_VERSION = 1;
