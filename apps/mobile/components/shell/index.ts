/**
 * Barrel export for shell components.
 *
 * Shell components are static UI (not SDUI-driven).
 * Per architecture: barrel per sub-folder, never a global components/index.ts.
 */

export { PairingScreen } from './PairingScreen';
